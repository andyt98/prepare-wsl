# Ansible playbook to prepare WSL

Prerequisites:
- Install git 
```
sudo apt install git
```
- Install ansible
```
mkdir ansible
python3 -m venv venv
source venv/bin/activate
pip3 install ansible
```
